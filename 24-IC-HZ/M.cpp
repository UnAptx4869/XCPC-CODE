#include<bits/stdc++.h>
using namespace std;
#define int long long
const int N = 1e5+5;
void solve(){
    int n,lim;
    cin >> n >> lim;
    vector<int>b(n);
    for(auto &x:b) cin >> x;
    

}
signed main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    int times;cin >> times;
    while(times--) solve();
    return 0;
}
